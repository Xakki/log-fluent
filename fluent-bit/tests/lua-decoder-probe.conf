[SERVICE]
    Log_Level error

[INPUT]
    Name  dummy
    Tag   probe
    Dummy {"message":"synthetic"}

[FILTER]
    Name   lua
    Match  probe
    script /fluent-bit/etc/tests/lua-decoder-probe.lua
    call   json_decoder_probe

[OUTPUT]
    Name  stdout
    Match probe
