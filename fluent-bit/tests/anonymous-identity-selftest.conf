[SERVICE]
    Log_Level error
    Exit_On_Eof true

[INPUT]
    Name  stdin
    Tag   probe

[FILTER]
    Name   lua
    Match  probe
    script /fluent-bit/etc/tests/anonymous-identity-selftest.lua
    call   anonymous_identity_selftest

[OUTPUT]
    Name stdout
    Match probe
